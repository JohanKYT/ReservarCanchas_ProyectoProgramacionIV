@model IEnumerable<ReservarCanchas_ProyectoProgramacionIV.Models.Calendario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaFin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NotasDetallada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cancha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NotasDetallada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cancha.CanchaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CalendarioId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CalendarioId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CalendarioId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
